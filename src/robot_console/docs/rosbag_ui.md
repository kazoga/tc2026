# PC側UIのROSBAG保存

ダッシュボードの「ROSBAG保存」で保存先を選び、**記録開始**を押す。
終了時は **停止・保存** を押し、「保存完了」になるまで待つ。
記録状態、経過時間、保存容量、保存先と出力フォルダ名を表示する。
空き容量は状態欄、出力先の完全なパスはフォルダ名のツールチップで確認できる。
Web UIには操作・状態表示を追加していない。

既定の保存先はPCの `~/rosbags`。環境変数 `ROBOT_CONSOLE_BAG_DIR` で初期値を変更でき、
UIの保存先欄または「変更…」でも変更できる。記録中は保存先変更・二重開始を無効にする。
同じ保存先を使用する別UIとの同時記録もロックで防ぐ。
日時とランダムIDのフォルダを毎回作り、既存bagを上書きしない。

`ros2 bag record --all-topics --storage sqlite3 --max-bag-size 1073741824` を起動する。
通常の全トピックが対象で、開始後に出現するトピックも探索する。
隠しトピックとサービスは対象外。ROS_DOMAIN_ID等はUIの実行環境を引き継ぐ。
センサ・点群・画像を含むため、実機で必要な受信レートと保存媒体の速度を確認すること。
bagは約1 GiBごとのdb3ファイルとmetadata.yamlで構成される。親フォルダに同名.logも残す。

停止はSIGINTを送り、プロセス正常終了とmetadata.yamlの存在を確認して保存完了とする。
UIの通常終了・SIGINT/SIGTERM終了時にも停止処理を行う。
20秒以上終了しない場合はSIGTERMを送り、正常保存とは扱わない。
強制電源断・SIGKILLでは最終保存を保証できない。
開始時に空き1 GiB未満なら開始せず、記録中512 MiB未満になったら停止する。
停止・エラーで保存済みファイルを自動削除しない。

## 検証

関連100テストに合格。ROS_DOMAIN_ID=193の隔離した模擬topicを実際のrosbag2で記録し、
停止後metadata.yamlとSQLite内のメッセージ存在を確認した。
二重開始、別UIとの競合、空き不足、ros2未導入、UIボタン状態も確認した。
実機センサ一式の帯域・長時間保存は未検証。
画面の静止画像は `log/codex/rosbag_ui_20260920/` に保存（表示例は模擬値）。
これらPC内パスはiPhone向け共有URLではない。

実装はcore/bag_recorder.py、Qt表示はui_qt/widgets/bag_card.py、試験はtools/testsに置き、
既存の状態管理・表示・テストの分離に合わせた。
