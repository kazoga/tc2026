# ROS 2 Python パッケージで使用する pip モジュール。
# rclpy, cv_bridge, 各種 msg などの ROS パッケージは rosdep/apt で導入する。
# obstacle_route_sim の Gazebo / ros_gz 依存は pip ではなく rosdep/apt で導入する。
catkin_pkg>=1.0,<2.0
# ROS 2 Jazzy の rosidl テンプレート展開は empy 4.x で失敗するため 3.3.4 に固定する。
empy==3.3.4
japanize-matplotlib>=1.1,<2.0
lark>=1.1,<2.0
matplotlib>=3.8,<4.0
ncnn>=1.0,<2.0
networkx>=3.2,<4.0
# ROS 2 Jazzy の cv_bridge は NumPy 1.x ABI 前提のため、NumPy 2.x は使用しない。
numpy>=1.26,<2.0
# opencv-python 4.12 以降は NumPy 2.x を要求するため、cv_bridge 互換の範囲に固定する。
opencv-python>=4.10,<4.12
pandas>=2.1,<3.0
Pillow>=10.0,<13.0
pyserial>=3.5,<4.0
# route_planner, route_manager, robot_console, obstacle_route_sim の YAML 設定で使用する。
PyYAML>=6.0,<7.0
requests>=2.31,<3.0
# ament_python の --symlink-install で setuptools 81.x は --editable 未認識となるため上限を置く。
setuptools>=65.0,<69.0
ultralytics>=8.3,<9.0
